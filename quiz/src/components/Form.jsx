import styles from "./Form.module.css"
import React from 'react'

const Form = ({lista}) => {
    const {o}


  return (
    <div>
        <form>
            <input type="text" value={}  disabled/>
        </form>
    </div>
  )
}

export default Form